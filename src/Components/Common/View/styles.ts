import { SxProps } from "@mui/material";

const styles: {
  [key: string]: SxProps;
} = {
  label: {
    color: "#121110",
    fontSize: "16px",
  },

  value: {
    fontSize: "14px",
    color: "#818990",
  },
};

export default styles;
